# cc infinite canvas v0.1.7

## 优化

- 画布缩放范围从 25%-250% 放宽到 2%-2400%，更适合大画布排版。
- 生图节点默认更紧凑，调试参数默认收起，生成按钮移动到节点顶部右侧。
- 提示词和参数输入框支持鼠标滚轮滚动内容，不再误触画布缩放。

## 新增

- 支持在画布中直接粘贴剪贴板图片，截图或复制图片后按 `Ctrl+V` 即可加入画布。
- 保留节点复制粘贴逻辑：剪贴板没有图片时，`Ctrl+V` 仍粘贴已复制节点。

## 验证

- 已通过前端和服务端 Node 语法检查。
- 已通过 Git diff 空白检查。
