# cc无限画布 v1.1.3

`1.1.3` 新增独立 Midjourney 工作流，并完善云雾 Midjourney 的提示词限制和连续操作体验。

## Midjourney 独立节点

- 左侧工具栏新增 `MJ` 节点，使用云雾 Midjourney Proxy Plus 接口。
- 支持平台默认版本、V7、V6.1、Niji 7，以及 Fast、Relax、Turbo 模式。
- 支持本地上传、画布选图和连线添加参考图，单节点最多 5 张。
- 文字、随机肖像和场景机位节点可作为前缀、正文或后缀提示词连接。
- 生成结果会保存到当前画布缓存，避免只依赖上游临时链接。

## 提示词保护

- 根据云雾通道实测，将最终 Midjourney 提示词限制为 1800 字符。
- 字符计数包含正文、连接文字和自动追加的比例、版本、Stylize、Chaos、Seed 等参数。
- 超限时优先保留末尾参数，只自动裁剪正文，并在节点内显示计数提示。
- 前端和服务端都有长度保护，避免超长请求直接提交到上游后失败。

## 连续操作修复

- U1-U4、V1-V4、重新生成、强弱变化、局部重绘、缩放和方向扩图等按钮已汉化。
- 后续操作结果优先继承上一张 Midjourney 输出图的位置。
- 修复旧工程输出锚点为 `(0,0)` 时，二次结果跑回画布原点的问题。

## 安装说明

- Windows 安装包：`YunwuImageCanvasSetup.exe`
- 支持覆盖安装并沿用旧版安装位置和缓存位置。
- 安装包不会包含本地 API Key、Access Key、Secret Key、画布缓存或生成结果。
