# cc无限画布 v1.1.5

`1.1.5` 集中发布近期完成的模型兼容、智能助手、剧本拆解、项目恢复与大型画布交互升级。

## 模型与平台

- 火山方舟新增 Seedance 2.5，使用官方 `doubao-seedance-2-5-260628` 模型 ID，支持 4-30 秒、480p/720p、原声音频和现有参考素材流程。
- 自定义中转站的异步媒体接口会持续轮询到最终结果，不再把仍在生成的任务误判为成品。
- 新增 Grok Build CLI 账号接入，可在设置中安装、登录、切换账号，并将可用对话、图片和视频模型接入对应节点与助手。
- DeepSeek 官方 API 使用独立设置页，可单独配置 Key、Base URL 和模型。
- 多模型连接继续支持各模型独立平台、Key、协议、接口路径和模型 ID。

## 智能助手

- 助手可自动识别画布操作意图并生成、应用计划，也可以关闭自动应用。
- 选中画布图片后创建生图节点时，可自动附加参考图；自动生成开关开启后无需再次点击应用计划。
- 生图、视频和 Midjourney 生成任务支持停止，主动取消不会显示为生成错误。
- 助手计划执行、模型返回和重试流程增加兼容处理，减少 JSON 代码直接显示或已有计划吞掉后续计划的问题。

## 剧本拆解

- 新增剧本资产拆解节点，可导入剧本、使用大语言模型拆解人物、场景、道具等资产，并分别创建资产图、分镜图和视频节点。
- 折叠卡片会显示子节点进度，可一键创建全部节点、重试失败任务，并标记旧版拆解产生的节点。
- 资产生图按资产类型保存画幅意图；人物默认使用 21:9 特写加正面、侧面、背面三视图组合，场景、道具、服装、载具和生物使用各自默认比例。
- 分镜生图继续使用统一成片画幅，避免后续镜头和视频比例不一致。
- 大剧本分析使用更长超时和更大的输出预算，并根据输入长度收缩输出规模。

## 项目与画布

- 新增删除当前画布和批量删除画布，删除前必须二次确认，并同步清理对应本地缓存。
- 增强异常断电或未正常关闭后的项目恢复，启动时会从备份和自动保存记录恢复画布。
- 大型画布继续使用媒体虚拟化和分级缩略图；拖拽与滚轮事件按帧合并，减少全量 DOM 重建和强制布局。
- 生成结果保持在任务节点附近，节点连线、选中状态和媒体显示更稳定。

## 安全与稳定性

- Photoshop 本地桥限制允许的来源并在预检前完成校验。
- 自定义连接不再把已保存的 API Key 发送到不同最终地址，同时校验最终请求 URL 的协议与主机。
- Windows 即梦 CLI 安装包增加 SHA-256 校验。
- 安装包已包含 Grok Build 运行桥，避免安装版因缺少运行文件而无法启动。

## 安装说明

- Windows 安装包：`YunwuImageCanvasSetup.exe`
- 支持覆盖安装并沿用旧版安装位置和缓存位置。
- 安装包不会包含本地 API Key、Access Key、Secret Key、画布缓存或生成结果。
