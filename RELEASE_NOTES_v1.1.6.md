# cc无限画布 v1.1.6

`1.1.6` 重点升级 Seedream 5.0 Pro 的画布工作流、图片图层控制，以及自定义模型连接和桌面端稳定性。

## Seedream 5.0 Pro

- 新增智能拆图层模式。官方模型和包含 Seedream 5 Pro、layer decomposition 等关键词的自定义渠道均可自动识别，也支持拆图／融图与普通生图分开的专用模型。
- 拆图层请求会自动启用 `layer_decomposition`，限制为一张输入图，并解析底图、透明 PNG 部件、名称、描述、层级及定位信息。
- 拆分结果会按原图尺寸、坐标和缩放比例恢复到画布；透明部件保持真实 Alpha 通道，不再显示不透明底色。
- 新增画布多图融合：直接使用图片当前位置、大小与前后遮挡关系生成构图稿，并保留原图作为细节参考。
- 多图融合及手动添加参考图均按画布“底层到顶层”的顺序读取。

## 图片图层与画布交互

- 选中图片后可执行置底、下移一层、上移一层和置顶，多选图片会整体移动并保持组内相对顺序。
- 新增快捷键：`[` 下移、`]` 上移、`Shift + [` 置底、`Shift + ]` 置顶；在输入框内不会误触。
- 拖动图片不再自动置顶，选中状态也不再通过临时超高 z-index 伪装成顶层，画面始终显示真实遮挡关系。
- 智能拆分的部件标签改为仅在悬停或选中时显示，减少大批图层同时铺开时的视觉干扰。

## 模型与平台连接

- 自定义视频中转模型会按用途进入视频节点模型列表，并兼容方舟异步视频任务接口。
- 加强 Gemini 原生生图协议及自定义中转配置，模型自身的 Base URL、接口路径、模型 ID 和专用 Key 可独立生效。
- 云雾相关旧连接可迁移到新服务地址，同时保持其他渠道配置不变。
- Midjourney 结果保存时会根据响应 MIME、文件签名和 URL 自动补齐正确扩展名，避免生成图片被保存为无后缀文件。

## 桌面端与稳定性

- Windows 桌面版优先使用安装包内置 Electron 运行时，开发环境缺少依赖时给出明确修复提示，覆盖安装后可长期稳定启动。
- 撤销画布操作不再中断正在进行的生图请求；生成节点及请求控制器在历史恢复期间保持有效。
- 新增拆层布局、图层排序与生成状态隔离回归测试，并继续覆盖模型协议、媒体虚拟化、方舟、Midjourney 和助手流程。

## 安装说明

- Windows 安装包：`YunwuImageCanvasSetup.exe`
- 支持直接覆盖旧版本，并沿用原安装目录、模型连接、API Key、画布缓存和生成结果。
- 安装包不会包含开发机上的 API Key、Access Key、Secret Key、画布缓存或生成文件。
